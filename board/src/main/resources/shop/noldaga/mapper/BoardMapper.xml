<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="shop.noldaga.mapper.BoardMapper">
	<sql id="criteria">
		<trim prefix="(" suffix=") and " prefixOverrides="or">
			<foreach collection="typeArr" item="type">
				<trim prefix="or">
					<choose>
						<when test='type == "T"'>
							title like '%${keyword}%'
						</when>
						<when test='type == "C"'>
							content like '%${keyword}%'
						</when>
						<when test='type == "W"'>
							writer like '%${keyword}%'
						</when>
					</choose>
				</trim>
			</foreach>
		</trim>
	</sql>
	
	<select id="getTotalCount" resultType="int">
		select count(*) from tbl_board where 
		<include refid="criteria"/> 
		bno > 0
	</select>
	
	<select id="getList" resultType="shop.noldaga.domain.BoardVo">
		<![CDATA[
			select * from tbl_board where bno > 0
		]]>
	</select>
	
	<select id="getListWithPaging" resultType="shop.noldaga.domain.BoardVo">
		<![CDATA[
		select * from (
		    select /*+ index_desc(t pk_board) */ t.*,rownum rn 
		    from tbl_board t
		    where 
		]]>
		
		<include refid="criteria"/>
			
		<![CDATA[
		    	rownum <= #{pageNum} * #{amount}
		)
		where rn > (#{pageNum}-1) * #{amount}
		]]>
	</select>
	
	<insert id="insert">
		insert into tbl_board (bno, title, content, writer) values (seq_board.nextval, #{title}, #{content}, #{writer})
	</insert>
	
	<insert id="insertSelectKey">
		<selectKey keyProperty="bno" order="BEFORE" resultType="long">
			select seq_board.nextval from dual
		</selectKey>
		insert into tbl_board (bno, title, content, writer) values (#{bno}, #{title}, #{content}, #{writer})
	</insert>
	
	<select id="read" resultType="shop.noldaga.domain.BoardVo">
		select * from tbl_board where bno = #{bno}
	</select>
	
	<delete id="delete">
		delete from tbl_board where bno = #{bno}
	</delete>
	
	<delete id="update">
		update tbl_board set 
		title = #{title},
		content = #{content},
		writer = #{writer},
		updatedate = sysdate
		where bno = #{bno}
	</delete>
	
	<update id="updateReplyCnt">
		update tbl_board set replycnt = replycnt + #{amount} where bno = #{bno}
	</update>
</mapper>